import { createFileRoute } from "@tanstack/react-router"; import { Search,Download } from "lucide-react"; import { AppShell } from "@/components/app/app-shell"; import { StatusBadge,SectionCard,head } from "@/components/app/shared"; import { activities } from "@/lib/mock-data"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { toast } from "sonner"; import { Toaster } from "@/components/ui/sonner";
export const Route=createFileRoute("/activite")({head:()=>head("Journal d’activité","Traçabilité des actions utilisateurs, agents et système."),component:Page});function Page(){return <AppShell title="Journal d’activité" subtitle="Traçabilité complète des actions humaines et automatisées."><SectionCard title="Toutes les activités" action={<div className="flex gap-2"><div className="relative"><Search className="absolute left-2 top-2 size-4 text-muted-foreground"/><Input className="h-8 pl-8" placeholder="Rechercher une action..."/></div><Button size="sm" variant="outline" onClick={()=>toast.success("Journal exporté")}><Download/>Exporter</Button></div>}><div className="overflow-auto"><table className="w-full text-left text-xs"><thead><tr className="border-b bg-muted/50">{["Date","Utilisateur / Agent","Action","Module","Objet","Statut"].map(h=><th className="px-4 py-3" key={h}>{h}</th>)}</tr></thead><tbody>{activities.map((a,i)=><tr key={i} className="border-b hover:bg-muted/30"><td className="px-4 py-3 whitespace-nowrap text-muted-foreground">{a.date}</td><td className="px-4 font-bold">{a.actor}</td><td className="px-4">{a.action}</td><td className="px-4">{a.module}</td><td className="px-4 font-mono text-[10px]">{a.object}</td><td className="px-4"><StatusBadge tone="success">{a.status}</StatusBadge></td></tr>)}</tbody></table></div></SectionCard><Toaster richColors/></AppShell>}
